#include <iostream>

using namespace std;

int main()
{
    std::string fullname;

    std::cout << "Steve jobs:"<< endl;
    std::getline(std::cin, fullname);

    std::string firstname = full.substr(0, spacePosition);
    std::string lastname = fullname.substr(spaceposition +1 );
    std:: cout << "Formatted name" << last time << firstname << std::endl;

    return 0,
